# Core (needed for dev, tests, and the offline MockLLM demo — all lightweight, CPU-only)
numpy>=1.24
scikit-learn>=1.3
networkx>=3.0

# --- Heavy deps below: only needed for the REAL experiment runs on Colab/Kaggle GPU. ---
# Install with:  pip install -r requirements.txt  &&  pip install .[full]
# torch>=2.1
# transformers>=4.44
# sentence-transformers>=2.7
# faiss-cpu>=1.7
# datasets>=2.19
# matplotlib>=3.7
# pandas>=2.0
